# MongoDB Connection
MONGO_URI=mongodb://localhost:27017

# Algorand testnet RPC & Indexer
ALGORAND_NODE=https://testnet-api.algonode.cloud
ALGORAND_INDEXER=https://testnet-idx.algonode.cloud

# Smart Contract App ID (deployed on testnet)
CONTRACT_APP_ID=757797076

# Farmer Pay Contract Address (for inner transactions)
# This is auto-calculated: encoding.encode_address(encoding.checksum(b"appID" + app_id.to_bytes(8, "big")))

# Pinata IPFS
PINATA_API_KEY=your_pinata_api_key_here
PINATA_SECRET=your_pinata_secret_here

# JWT Configuration
JWT_SECRET=your-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# Server Configuration
PORT=4000
FRONTEND_URL=http://localhost:5173
